# 命令：更新 PRD

对当前 PRD 进行变更，并自动维护版本记录：

$ARGUMENTS

## 使用方式

在已有 PRD 的文件夹内运行此命令，或通过参数指定 PRD 路径/标题。

`$ARGUMENTS` 描述本次变更内容，例如：
- "在业务规则章节补充登录态超时逻辑"
- "将验收标准 AC-2 的描述改为…"
- "删除页面 B，将其功能合并到页面 A"

## 执行步骤

**1. 定位目标 PRD**

按以下优先级查找：

1. `$ARGUMENTS` 包含标题或路径 → 先查 `prds/_registry.md` 匹配（路径可能为 `prds/[标题]/prd.md` 或 `prds/[ID]-[标题]/prd.md`）；未找到时在 `drafts/` 中搜索匹配的目录（兼容 `drafts/[标题]/prd.md` 和 `drafts/[ID]-[标题]/prd.md` 两种格式）
2. 两处都未找到 → 告知用户"未找到该 PRD，请确认标题是否正确"，停止执行

找到后记录 PRD 所在区域（`prds/` 或 `drafts/`），后续步骤据此操作。
读取 YAML frontmatter 获取当前版本号。

**2. 归档当前版本**
- 将当前 `prd.md` 完整复制到 `archive/prd-v[当前版本].md`
- 例如当前 version 为 "1.1"，则存为 `archive/prd-v1.1.md`

**3. 执行变更**
- 根据 `$ARGUMENTS` 的描述，精确修改 `prd.md` 中对应的章节内容
- 只改需要改的部分，不重写整个文件

**3.5 存量模板清理（PRD-UPD-010）**

执行完步骤 3 变更后，扫描 `prd.md` 全文，匹配以下已知模板指引模式：

| 匹配模式 | 说明 |
|---------|------|
| `> **[AI-ONLY]**` | 新标记格式（模板已更新后的 AI 指引行） |
| `> **使用场景**：` | 旧模板头部使用场景说明 |
| `> **复制方法**：` | 旧模板头部复制方法说明 |
| `> **编写规则**：` | 章节编写指引（含后续连续 blockquote 续行） |
| `> 端清单参考：` | context 文件引用提示 |
| `> 权限模型参考：` | context 文件引用提示 |
| `> 上线当天测试人员` | 验收标准编写指引 |
| 以 `# 功能 PRD 模板`/`# 史诗 PRD 模板`/`# 用户故事卡模板` 开头的行 | 旧版模板 H1 标题行 |
| YAML frontmatter 中的行内注释（`field: value  # 注释`） | 仅匹配 `---` 块内 |

处理规则：
- 删除所有匹配的行；对于连续 blockquote 块（首行匹配），删除后续所有续行直到遇到空行或非 `>` 开头行
- 清理因删除产生的多余空行（连续空行超过 1 行时合并为 1 行）
- 记录清理数量（每个独立匹配块计为 1 处）和内容摘要

步骤 7 输出确认中追加清理结果：
- **有清理**：「已清理 N 处模板残留文字：」+ 各处内容摘要（截取前 40 字）
- **无清理**：静默跳过，不输出此条

**3.6 领域检查清单扫描（条件触发，原步骤 3.5）**

检查 `context/domain-checklists.md` 是否存在：
- **不存在**：输出提示「未检测到领域检查清单配置，如需启用可运行 /init 或手动创建 context/domain-checklists.md」，静默跳过
- **存在**：读取注册表（最多 10 行，超出警告并跳过），筛选 `扫描时机` 含 `update-prd` 的行，将变更描述（`$ARGUMENTS`）及已读取的 `prd.md` 内容与各行触发关键词逐一匹配：
  - **命中**（按需加载，每次执行最多触发 3 个清单）：读取对应清单文件（建议 ≥ 3 条检查项、≤ 150 行），执行扫描，在继续后续步骤前输出领域影响结论块（格式：有影响 / 无影响 / 待确认）
  - **未命中**：静默跳过

**4. 更新 YAML**
- `version`：小版本号 +0.1（如 1.1 → 1.2）；若本次是重大重构则升主版本（1.x → 2.0）
- `updated`：今天日期

**5. 追加 CHANGELOG**

在 `CHANGELOG.md` 顶部（最新记录在最前）插入：
```markdown
## v[新版本] — [今天日期]

**变更类型**：[内容补充 / 逻辑修改 / 结构调整 / 问题修复]

**变更内容**：[具体描述本次修改了什么]

**影响范围**：[受影响的章节、页面、字段或接口]
```

**6. 更新注册表（仅正式区 PRD）**

- 若 PRD 在 `prds/`：将 `_registry.md` 对应行的"最后更新"字段改为今天日期
- 若 PRD 在 `drafts/`：跳过此步骤（草稿尚未注册）

**7. 输出确认**

告知用户：
- 旧版本已归档至 `archive/prd-v[旧版本].md`
- 新版本号
- 本次修改的章节摘要

**7.5 关联文档同步提示（仅正式区 PRD，且存在关联文档时）**

读取 PRD YAML，若 `has-page-spec: true` 或 `has-prototype: true`，在输出确认后追加：

> PRD 已更新至 v[新版本]。如本次变更影响页面或交互，建议运行 `/sync-docs [标题]` 检查关联文档是否需要同步。

**原型过期提示（PROTO-LNK-001）**：额外检查 `outputs/prototypes/[标题]/prototype-meta.md` 是否存在：
- **存在**：读取 `prd-version` 字段，与 PRD 新版本比较，追加：
  > ⚠️ 该 PRD 关联了原型（版本 [关联版本]），当前 PRD 已更新至 [新版本]，建议检查原型是否需要同步更新：`/generate-prototype [标题]` 重新生成，或 `/sync-docs [标题]` 检查差异。
- **不存在但 `has-prototype: true`**：追加：
  > ⚠️ 该 PRD 关联了原型但版本信息缺失，建议重新运行 `/generate-prototype [标题]` 生成。

**8. context 文件同步检查（仅正式区 PRD，且类型为 feature 或 epic 时执行）**

读取本次变更中 **§5 功能结构**和 **§7 功能清单**的内容（排除 §12 开放问题和 TODO/待规划项），判断是否引入了新的功能模块或业务术语：

- **新增 L2 功能模块**（如本次变更新增了页面、模块或功能入口，且出现在 §5 或 §7 中）→ 提议更新 `context/product-feature-map.md` Mermaid 树和索引表：

  > 本次变更新增了以下功能模块，建议追加到 `context/product-feature-map.md`：
  > - Mermaid 树：[L1 能力域] 下追加 [模块名]
  > - 前缀映射表：[模块名] → [英文前缀]
  > - 索引表：[关键词列表]
  > 回复「确认」写入，或告知需要修改。

- **已有 L2 模块下新增子功能**（本次变更在 §7 中新增了功能点，但所属模块已存在）→ 仅更新索引表：

  > 本次变更在 [L2 模块名] 下新增了子功能，建议更新 `context/product-feature-map.md` 索引表：
  > - 追加关键词：[关键词列表]
  > - 关联 PRD 追加：[当前 PRD ID]
  > 回复「确认」写入，或告知需要修改。

  用户确认后更新 product-feature-map.md 对应区域。

- **新增业务术语**（如本次变更引入了 `context/business-glossary.md` 中尚未定义的专有名词）→ 提议追加：

  > 本次变更引入了以下新术语，建议追加到 `context/business-glossary.md`：
  > [术语表格]
  > 回复「确认」写入，或告知需要修改。

- 两者均无变化时，静默跳过，不提示。

**8.5 OpenAPI 飞轮扫描（OAPI-FLY-001）（仅正式区 PRD）**

1. 读取更新后 PRD 中所有 §8.x.10（或 §8.10）章节内容
2. 判断是否有实质内容（非「不涉及」、非空表格、非占位符）：
   - **有实质内容** → 进入步骤 3
   - **无实质内容** → 静默跳过，不输出任何提示
3. 检查 `context/api-registry.md` 是否有关联 API：
   - **有关联 API** → 输出同步提议：
     > **检测到接口变更（§8.10 有内容），建议同步 OpenAPI 规范。**
     > 变更摘要：[从 §8.10 提取的接口路径 + 变更类型]
     > 建议运行 `/update-openapi [api-name]` 执行同步。可回复「跳过」忽略此提议。
   - **无关联 API 或 api-registry.md 不存在** → 输出初始化提议：
     > **检测到接口变更（§8.10 有内容），但尚未建立 OpenAPI 规范文件。**
     > 建议运行 `/import-openapi [api-name]` 初始化规范。可回复「跳过」忽略此提议。

**9. 草稿区发布询问（仅当 PRD 在 drafts/ 时）**

> **前置判断**：Step 1 中已记录 PRD 所在区域。若 PRD 在 `prds/`（正式区），直接跳过本步骤，不询问、不执行移入操作。

仅当 PRD 在 `drafts/` 时执行以下流程：

> 草稿已更新完成。是否现在移入正式区（prds/）？
> - **A. 继续完善**：留在草稿区，稍后再发布
> - **B. 正式发布**：移入 `prds/`，注册并进入评审流程
>
> 请回复 A 或 B。

选 B 时：执行与 `/new-prd` 步骤 7B 相同的移入正式区流程——ID 冲突检测与自动顺延、将 `drafts/[ID]-[标题]/` 移动到 `prds/[ID]-[标题]/`、删除草稿目录、清理 `drafts/_draft-registry.md` 预注册条目、在 `prds/_registry.md` 追加注册行（路径格式 `prds/[ID]-[标题]/prd.md`）。

## 注意

- 不要修改 `archive/` 下的任何文件
- 如果变更描述模糊，先向用户确认具体修改内容，再执行
