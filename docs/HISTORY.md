# 项目演进日志

> 记录每次重要功能更新。遇到命令行为疑问时，在此查阅历史变更原因。

---

| 日期 | 事件 |
|------|------|
| （首次使用时填写） | 项目初始化 |
| 2026-04-10 | **串联流水线**：`/requirement-clarifier` 完成后自动保存 RDD 至 `drafts/[标题]/rdd.md` 并引导进入 `/new-prd`；`/new-prd` 新增步骤0（新功能/迭代识别）、步骤5.5（PRD细节两轮澄清，AI自判收敛）、移入正式区后自动判断是否涉及页面变更并引导后续步骤 |
| 2026-04-10 | **新增命令 `/generate-page-spec`**：PRD → 页面规格卡，作为原型生成的前置中间层，保存至 `prds/[标题]/page-spec.md`，写入 `source-prd-version` 和 `last-updated` 元数据 |
| 2026-04-10 | **新增命令 `/sync-docs`**：读取 PRD CHANGELOG 与关联文档元数据，比对版本差异，输出潜在不一致清单 + 建议操作；`/update-prd`、`/generate-prototype` 末尾同步加入轻量提示 |
| 2026-04-10 | **新增模板 `templates/iteration-prd.md`**：迭代类 PRD 精简模板，含 `modifies` 关联字段、变更前→变更后结构、只写变化部分的页面/规则章节；`/new-prd` 步骤0新增迭代类型识别和模板路由 |
| 2026-04-10 | **对话开场引导**：CLAUDE.md 顶部新增路径选择菜单（8条路径），模糊输入或触发词时展示，命令速查表和标准工作流同步更新 |
| 2026-04-15 | **PRD 输出质量升级**：(1) `/requirement-clarifier` 新增 Step 0 按需读取 6 个 context 文件、Step 5 输出 RDD 双节结构（需求摘要 7 维 + 初步方案 6 小节）、Step 6.5 业务术语提议；(2) `templates/feature-prd.md` 重写为 §1-§11 完整结构，含 §7 功能清单（[AREA]-[CAT]-[SEQ] 编号）和 §8 逐功能展开（§8.1-§8.9 条件填写）；(3) 新建 `templates/rdd.md` 作为 RDD 格式基准；(4) 新建 `context/business-glossary.md`（业务术语字典）和 `context/product-feature-map.md`（功能结构树 + 前缀映射表），由需求/PRD 流程联动维护；(5) `/new-prd` 新增 5a 功能编号生成、5b §8 展开指引、Step 7 后 context 同步提示；(6) `/update-prd` 新增 Step 8 context 同步检查；(7) `/ingest-prd` 新增 Step 9 context 同步；(8) `rules/prd-quality-gates.md` 新增 G7（功能清单规范）和 G8（§8 必填完整性） |
| 2026-04-16 | **数据飞轮完善 + 两阶段需求分析重构**：(1) `/requirement-clarifier` 重构为两阶段流程——Phase 1（仅读 user-persona + product-background + platform-support，生成用户故事，阻塞确认）→ Phase 2（对话信号驱动按需加载剩余 context，多轮澄清生成 RDD）；支持传入标题续接中断分析；(2) `templates/rdd.md` 新增 status/phase/context-loaded/story-version frontmatter 字段，rdd.md 改为渐进式写入（story-confirmed → rdd-in-progress → rdd-complete）；(3) `/new-prd` 新增 Step 5-0 rdd.md 状态检查（未完成时给出提示和选项），按 PRD 章节分步加载 context 文件；(4) 修复 `context/platform-support.md` 孤岛——接入 requirement-clarifier Phase 1 和 new-prd §9.4；(5) `/sync-docs` 新增 Step 5 context 一致性检查（术语孤岛 + 功能前缀未注册），飞轮由单向变双向；(6) `rules/prd-quality-gates.md` 新增 G9（平台兼容性，条件触发）；(7) 新增 `evals/integration/TC-workflow-chain.md`（8 个端到端集成测试），补充 TC-RC-09/10/11/12、TC-NP-16/17、TC-SD-07/08/09 |
| 2026-04-16 | **路由规则升级 + CLAUDE.md 瘦身**：(1) 意图匹配从"AI主观判断清晰度"改为"默认走①，4条通用信号+产品专属信号强制路由"；(2) 产品专属 X-Y 信号迁移至 `rules/routing-signals.md`；(3) CLAUDE.md 非运行时内容（PRD规范/目录说明/变更规范/演进日志）迁移至 `docs/` 独立文档，CLAUDE.md 从 ~353 行缩减至 ~241 行 |
| 2026-04-20 | **数据飞轮行为准则**：将 context 文件与 PRD 的双向联动从命令步骤提升为全局行为准则；新增 `rules/data-flywheel.md`（触发条件表、新术语/新功能节点认定标准、强制规则、一致性检查扩展）；CLAUDE.md 新增"数据飞轮准则"章节（触发表 + 读入规则 + 强制规则速查）；`rules/` 读取规则表补入 data-flywheel.md |
| 2026-04-20 | **路由信号强化 + 领域检查清单扫描**：(1) 已有功能迭代场景（"改一下XX"/"在XX基础上增加"）默认先走需求澄清，RDD 完成后再判断走 `/update-prd` 还是 `/new-prd iteration`；(2) 产品专属 X-Y 信号迁移至 `rules/routing-signals.md`，CLAUDE.md 路由规则引用该文件；(3) 新增通用领域检查清单扫描 skill，支持 requirements / design / data-model 三个阶段；(4) `context/business-glossary.md` 新增 `type` 列（产品概念 / 枚举值 / 系统字段），区分术语分类 |
| 2026-04-22 | **F-003 ～ F-008 全面落地**：(1) F-003 新增 `/import-context` 命令——支持文字/截图/文档多形态导入，AI 分类建议后确认写入对应 context 文件，缺口检测提示未覆盖内容；(2) F-004 命令路由增强——`/requirement-clarifier` 多候选路径硬约束、命令纠错流程、开场菜单逻辑优化；(3) F-005 原型生成升级——无规格卡时强制阻断并引导先执行 `/generate-page-spec`，支持多页面结构和截图还原，写入版本关联元数据；(4) F-006 文档同步升级——新增内容级对比（不只比版本号）、飞轮待处理项检测，输出分差异清单和建议操作两块；(5) F-007 知识库提问升级——意图识别（澄清/设计/规格/边界）→ 四层检索（PRD → RDD → 规则 → 上下文）→ 来源标注，无法回答时提示补充；(6) F-008 技能管理——自建技能创建引导、三级质检（格式/行为/集成）、技能注册和基础管理操作；(7) `evals/` 集成测试用例全面重写，覆盖 9 场景 19 用例（F-001 ～ F-008 全链路） |
| 2026-04-23 | **PRD 生命周期一致性（F-009）**：(1) `/new-prd` 文件夹命名统一为 `[ID]-[标题]/`，与 `/ingest-prd` 对齐；(2) 草稿创建时向 `drafts/_draft-registry.md` 预注册 ID，防止并发重号；(3) 移入正式区时自动检测 ID 冲突并顺延，同步更新文件内引用；(4) 移入正式区后自动删除 `drafts/[ID]-[标题]/` 目录，消除冗余残留；(5) 新增 `/abandon-prd [标题]` 命令，主动放弃草稿并释放预注册 ID；(6) 新增 `templates/migration-plan.md`，提供存量文件夹一次性迁移步骤；(7) 废弃 `templates/iteration-prd.md`，迭代优化统一使用 `feature-prd.md`（步骤 0 中选迭代模板路径）；(8) 新增 `TC-prd-migrate` 迁移专项测试用例 |
| 2026-04-27 | **PRD 输出净化（F-012）**：(1) 模板三件套（feature-prd / epic-prd / story-card）新增 `[AI-ONLY]` 标记共 20 处，区分 AI 写作指引与读者内容；(2) `/new-prd` 步骤 3a 新增净化规则（PRD-GEN-020：剥离 `[AI-ONLY]` 行含多行块；PRD-GEN-021：剥离 YAML 行内注释），写入 prd.md 前自动执行；(3) `/update-prd` 新增步骤 3.5 存量模板残留扫描清理（PRD-UPD-010），识别并删除已知残留模式，输出清理摘要；(4) `rules/prd-quality-gates.md` 新增 G11 模板净化检查组（G11-01/02/03，全部 ❌ 必须）；(5) `docs/contributing.md` 新增模板变更额外规范：AI 指引内容必须加 `[AI-ONLY]` 标记；(6) 新增通用研发流程 Playbook `docs/feature-dev-playbook.md`（5 阶段模板）；(7) 新增 TC-NP-27/28/29 和 TC-UP-05/06/07（净化规则验证） |
| 2026-04-27 | **需求池管理（F-011）+ 质量门禁前置（F-010）**：(1) 新增 `/backlog` 命令——自然语言意图路由（录入/查看/排序/状态更新/扫描/排期/初始化/澄清 8 种意图），支持四种条目类型（新需求 REQ- / 行动项 ACT- / 开放问题 OQ- / 技术债 TD-），含产品线隔离检查（BR-05）、重复检测、最小输入模式、终态归档提议；(2) 飞轮扩展 3 条触发：BKL-FLY-001（PRD 移入正式区后扫描 §12+TODO 提议入池）��BKL-FLY-002（PRD 更新后扫描增量 TODO/OQ 提议入池）、BKL-FLY-003（RDD/PRD 状态变化提议更新需求池条目状态）；(3) `/new-prd` 移入正式区前新增质量门禁检查（`rules/prd-quality-gates.md`），❌ 项阻断移入；(4) 标准工作流扩展——需求池作为 RDD 前置阶段的统一收口区，`/backlog` 排期衔接 roadmap-planning 技能；(5) 新增 `evals/commands/TC-backlog.md`（16 用例）和集成测试场景十（TC-INT-21~23） |
| 2026-04-30 | **JTBD/需求澄清边界拆分 + Feature-map 双视图重构**：(1) 修复数据飞轮幻觉风险（ACT-001）：`rules/data-flywheel.md` 新增排除规则（§12 开放问题、TODO/待规划项不得触发 feature-map 写入）；`context/product-feature-map.md` 重构为双视图（Mermaid L2 全景 + 模块功能索引表 L3 关键词）；`/new-prd` 和 `/update-prd` 飞轮写回分两层（新 L2 模块→Mermaid+索引表，L2 子功能→仅索引表）；(2) `/requirement-clarifier` 命名去歧义——副标题改为"问题诊断 → RDD 生成"，移除"JTBD 框架"命名混淆；(3) Phase 1 X-Y 诊断新增 P1-3b 商业/定位层信号检测，Phase 2 多轮澄清新增每轮商业层升级检测——命中时推荐转 `jobs-to-be-done` skill，不强制中断；(4) JTBD skill Related Skills 交叉引用更新；(5) CLAUDE.md 决策地图和路由边界表更新；(6) 新增 TC-RC-10/11（商业层检测用例）；(7) 新建 `docs/release-cycle-playbook.md`：完整 10 阶段发布闭环模板（含 codex-support 专属研发/测试/发布步骤） |
| 2026-05-07 | **prd-qa 问答增强（F-013）**：(1) KQA-RTG-001 自然语言触发——"我想问产品问题"等模糊触发词不再直接检索，输出引导语等待用户说出具体问题；(2) KQA-QRY-004 问题清晰度收敛——无锚点问题追问最多 3 轮，达上限后标注精确度警告；(3) KQA-QRY-005 多意图顺序处理——检测 2+ 意图时拆分有序列表逐一回答，单次最多 3 个；(4) KQA-ANS-002 来源标注强制约束——有文档命中必须输出来源，无命中走兜底声明，不推断、不生成来源；(5) KQA-GDE-001 启发式追问引导——答案末尾追加 2-3 条完整问句建议，范围限定同一 PRD；(6) BR-01 跨功能业务规则——改进意图 > 查询意图，CLAUDE.md 路由和命令入口统一遵循；(7) 排除边界明确——代码/技术/竞品/改写意图不触发 prd-qa；(8) 同步更新 CLAUDE.md 路由规则和斜杠命令速查表 |
| 2026-05-11 | **页面截图库与导航关系图优化（F-015）**：(1) `/import-context` 截图库流程重构——九步子流程（CTX-IMP-004~008 + CTX-NAV-001~003）：前置 assets 目录建议（CTX-IMP-004）、展示已有节点编号列表（CTX-IMP-005）、触发方式收集（CTX-IMP-006）、父节点缺失占位节点处理（CTX-IMP-007）、原始文件清理询问（CTX-IMP-008）；(2) 双层导航图架构：全局索引 `context/page-navigation.md`（锚点级，≤50 行）+ 模块详情 `context/screenshots/[模块]/navigation.md`（完整 Mermaid + 截图索引表），节点形状区分（完整页面=方形，弹窗/抽屉=圆角）；(3) 信号驱动加载（CTX-NAV-004/005）：UI 关键词→加载全局索引，模块名→追加加载模块详情文件；(4) 数据飞轮扩展：CTX-FLY-005（`/update-prd` 或 `/new-prd` 移入正式区后扫描 §8 新页面，末尾追加补录建议）、CTX-FLY-006（`/generate-prototype` 完成后对比原型页面与导航图节点，有缺口时提示）；(5) 新增 `evals/commands/TC-import-context.md`（14 个测试用例 TC-IC-01~14），集成测试追加场景十三（TC-INT-25~27） |
| 2026-05-07 | **OpenAPI 文档集成（F-014）**：(1) 新增 `/import-openapi [api-name]` 命令——正式区门控（PRD 须在 prds/ 才可执行）；首次运行自动初始化 `openapi/` 目录（含 `_hidden-interfaces.md` 双粒度隐藏清单模板、`context/api-registry.md` 模块分组注册表、`outputs/openapi/`）；Path A（新建规范）：询问来源（已有文件导入或从 §8.10 生成），展示接口摘要后需 PM 确认才写入；Path B（差异比对）：有差异时推荐先走需求澄清，直接导入时 changelog 标注"无关联 PRD §8.10 变更记录"；(2) 新增 `/update-openapi [api-name]` 命令——读取 yaml 头部 changelog 去重，只处理 PRD §8.10 中未同步条目，生成增量 diff 后 PM 确认再执行；废弃接口提示加入 `_hidden-interfaces.md`；(3) 新增 `/export-openapi [api-name] [version?]` 命令——两阶段过滤（接口级：移除整条 endpoint；参数级：从保留 endpoint 中移除指定字段）；过滤后执行敏感内容扫描（内网地址/系统名/密钥样例），命中须明确回复"已检查，可以导出"；支持版本快照（同时写入 `[api-name]-public-v[version].yaml` 和 latest 文件）；内部版不被修改；(4) `templates/feature-prd.md` 各 §8.x 末尾新增 §8.10 API 接口变更说明（含飞轮触发提示）；(5) OAPI-FLY-001 飞轮：`/update-prd` 完成后自动扫描 §8.10，有内容且 PRD 在正式区时输出 OpenAPI 同步提议（含变更摘要），提供"跳过"选项；(6) `/sync-docs` 新增 Step 2.6 OpenAPI 接口同步检查（SYNC-OAPI-001）：从 api-registry.md 定位关联 API，对比 yaml changelog 与 PRD 当前版本，落后时报确定差异并建议 `/update-openapi`；(7) CLAUDE.md 同步更新 4 处（命令速查表/信号词/飞轮表/标准工作流）；(8) 新增 `evals/commands/TC-openapi-integration.md`（10 个测试用例 TC-OA-01~10）、集成测试 TC-WF-OA-01（3 步骤端到端链路），`TC-sync-docs.md` 追加 TC-SD-17~19 |
| 2026-05-15 | **OpenAPI 工具链治理（F-014 V1.1→V1.2）**：(1) `/import-openapi` 新增忠实导入约束（OAPI-IMP-FAITHFUL）——字段级三项充足性检查（描述非空/字段名清晰/分组完整），充足时强制引用原文不改写；新增 Webhook 回调事件识别（写入 x-webhooks 扩展）和错误码表识别（写入 x-error-codes 扩展），公共结构提取到 components/schemas 复用；(2) `/export-openapi` 新增 Webhook 级过滤（Step 2b）、版本号语义化格式校验+破坏性变更检测（Step 5b）、MD 文档联动生成（Step 5c，基于 `templates/openapi-md-template.md` 模板，示例严格从 yaml example 读取）；(3) `/update-openapi` 新增反向同步检测（Step 1b，OAPI-SYNC-001）——检测"直接导入"changelog 条目，提议补充关联 PRD §8.10；(4) 新增 `templates/openapi-md-template.md`（对外 MD 文档模板，覆盖 REST endpoint + Webhook + 错误码三部分）；(5) `/update-prd` Step 9 修复正式区路径判断（REQ-006）；(6) 新增 TC-OA-11~21（11 个测试用例覆盖 AC-12~AC-20） |
| 2026-05-15 | **PRD 移入正式区 ID 冲突修复**：(1) `/new-prd` Step 7 选 B 新增步骤 0（远程 registry 同步）——执行 `git fetch origin` 后读取远程 `prds/_registry.md`，与本地合并取并集作为冲突检测基准，解决多人并发 push 导致 ID 重复的问题；git 失败时降级为本地检测并输出警告；(2) ID 冲突修复范围从"仅 prd.md frontmatter + CHANGELOG.md"扩展为全量替换：prd.md 全文（含 §1 元数据表格 PRD-ID 行）、CHANGELOG.md、rdd.md（若存在）、fields.md（若存在）、context/api-registry.md 关联 PRD 列（若存在）、context/page-navigation.md（若存在）、草稿目录重命名；(3) 冲突提示新增实际更新文件列表；(4) Step 7 步骤 2-6 统一使用"最终 ID"（冲突顺延后的 ID）；(5) 新增 TC-NP-30（远程 registry 同步测试用例）；扩展 TC-NP-23 检查要点（覆盖 rdd.md、fields.md、api-registry、page-navigation、目录名、prd.md 正文） |
