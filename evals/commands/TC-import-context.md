# 测试用例：截图导入导航图（F-015 页面截图库与导航关系图优化）

> 关联 PRD：`prds/F-015-页面截图库与导航关系图优化/prd.md`
> 关联命令：`.claude/commands/import-context.md`（截图库流程步骤 4）
> 前置条件：除各用例单独说明外，默认 `context/page-navigation.md` 已存在且含至少一个锚点节点（首页）；`context/screenshots/发起流程/navigation.md` 已存在含两个页面节点

---

## TC-IC-01 前置引导：PM 表示导入截图 → 先输出 assets 整理建议

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-IMP-004 |
| **覆盖 AC** | AC-1 |
| **测试目标** | PM 表达导入截图意图时，AI 先给出 assets 目录整理建议，不阻断后续流程 |

**测试输入**
```
我要导入一张截图，这是发起签署的首页
```

**预期行为**
1. AI 识别为截图导入意图
2. 在收集模块信息前，先输出提示：「建议先将截图整理至 `assets/[功能名]/` 目录，再进行导入，便于后续追溯」
3. 提示后继续进入截图导入流程，询问模块归类或直接接受截图
4. 提示为建议性，PM 直接粘贴截图也可继续

**检查要点**
- [ ] 输出了 assets 目录整理建议
- [ ] 建议提示在询问模块之前出现
- [ ] 提示为建议性，不阻断后续流程
- [ ] 提示包含 `assets/[功能名]/` 路径格式

---

## TC-IC-02 父页面收集：AI 识别截图 → 展示已有节点列表 + Other 选项

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-IMP-005 |
| **覆盖 AC** | AC-2 |
| **测试目标** | AI 识别内容为页面截图后，展示当前导航图已有节点列表并包含 Other 选项 |

**前置条件**
- `context/page-navigation.md` 存在，含「首页」锚点节点
- `context/screenshots/发起流程/navigation.md` 存在，含「发起页」「签署人选择页」两个节点

**测试输入**
```
（粘贴一张截图）这是发起流程里的文件上传页
```

**预期行为**
1. AI 识别内容为页面截图，确认所属模块为「发起流程」
2. 读取已有节点：从 page-navigation.md 和 发起流程/navigation.md 提取节点名
3. 输出节点列表（含编号），末尾追加「Other（自行输入）」选项：
   - 1. 首页（全局索引）
   - 2. 发起页（发起流程）
   - 3. 签署人选择页（发起流程）
   - Other（自行输入）
4. 等待 PM 选择

**检查要点**
- [ ] 展示了所有已有节点（含来自全局索引和模块文件的节点）
- [ ] 节点带编号，PM 可直接回复编号
- [ ] 列表末尾含「Other（自行输入）」选项
- [ ] 等待 PM 选择，未自动进入下一步

---

## TC-IC-03 写入预览：父页面确认 → 写入节点和索引行 → 输出预览

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-002, CTX-NAV-003 |
| **覆盖 AC** | AC-3 |
| **测试目标** | PM 完成父页面和触发方式填写后，AI 输出写入预览（含正确节点形状），确认后写入 |

**测试输入**（接续 TC-IC-02，PM 选择父页面后）
```
2（发起页）
```
（AI 继续询问触发方式）
```
点击「上传文件」按钮
```

**预期行为**
1. AI 询问：「从发起页进入此页面的触发方式是什么？」
2. PM 输入触发方式
3. AI 判断截图为完整页面（独立 URL），选择方形节点
4. AI 输出写入预览：
   - Mermaid 新增片段：`launch-page -->|点击上传文件按钮| file-upload[文件上传页\n文件上传页.png]`
   - 截图索引新增行：`| context/screenshots/发起流程/文件上传页.png | 文件上传页 | 发起页 | 点击「上传文件」按钮 |`
5. 输出「以下内容将写入 发起流程/navigation.md，确认写入？」
6. PM 确认后写入文件

**检查要点**
- [ ] 完整页面使用方形节点 `[名称\n截图文件名.png]`
- [ ] 节点 ID 为英文短横线格式，无空格中文
- [ ] 输出了写入预览（Mermaid 片段 + 索引行）
- [ ] 等待 PM 确认后才写入文件
- [ ] 写入后 `navigation.md` 包含新节点和连边

---

## TC-IC-03b 弹窗节点：截图为弹窗 → 使用圆角节点

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-003 |
| **覆盖 AC** | AC-3（节点形状部分） |
| **测试目标** | 截图为弹窗/抽屉时，AI 使用圆角节点 `([...])` |

**测试输入**（截图为弹窗）
```
（粘贴截图）这是点击「签署」后弹出的签署确认弹窗
```

**预期行为**
1. AI 判断内容为弹窗（叠加层）
2. 写入预览中使用圆角节点：`launch-page -->|点击签署| sign-confirm([签署确认弹窗\n签署确认弹窗.png])`

**检查要点**
- [ ] 弹窗使用圆角节点 `([名称\n截图文件名.png])`
- [ ] 与完整页面方形节点区分明确

---

## TC-IC-03c 首个入口页：模块首次导入 → 同步更新全局索引

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-001 |
| **覆盖 AC** | AC-3（全局索引更新部分） |
| **测试目标** | 截图为新模块首个入口页时，AI 同步在全局 page-navigation.md 中挂到锚点节点下 |

**前置条件**
- 导入「企业偏好设置」模块的第一张截图
- `context/screenshots/企业偏好设置/navigation.md` 不存在（新模块）

**测试输入**
```
（粘贴截图）这是企业偏好设置的主页面，从 B 端首页的「设置」按钮进入
```

**预期行为**
1. AI 判断这是新模块（企业偏好设置）的首个截图
2. 写入预览中包含两部分：
   - 模块文件：写入 `context/screenshots/企业偏好设置/navigation.md`（新建）
   - 全局索引：在 `context/page-navigation.md` 中追加 `B端首页 -->|点击设置| 企业偏好设置`
3. 写入完成后全局索引行数未超过 50 行

**检查要点**
- [ ] 写入预览包含全局索引更新部分
- [ ] 全局索引追加了模块入口连边
- [ ] 模块 `navigation.md` 被创建（含标准文件头 + Mermaid 块 + 索引表头）
- [ ] 全局索引超过 50 行时输出警告（本用例验证未超出时不输出）

---

## TC-IC-04 父节点缺失：Other 输入未知页面 → 默认阻断 → PM 坚持则创建占位节点

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-IMP-007 |
| **覆盖 AC** | AC-4 |
| **测试目标** | PM 输入的父页面不在导航图中时，AI 默认阻断；PM 坚持跳过时创建占位节点 |

**测试输入**（PM 选择 Other 后）
```
合同详情页
```
（AI 检测到「合同详情页」不在导航图中，输出阻断提示）
```
先跳过，直接导入
```

**预期行为**
1. AI 输出阻断：「父页面 合同详情页 尚未在导航图中，建议先导入父页面截图以保证导航图的连通性。继续导入当前截图，还是先去导入父页面？」
2. PM 回复跳过
3. AI 在 `navigation.md` 中创建占位节点：`contract-detail[合同详情页\n待补充截图]`
4. 建立连边：`contract-detail -->|[触发方式]| current-page[...]`
5. 输出末尾追加：「占位节点 合同详情页 已创建，标注「待补充截图」，建议后续补录」

**检查要点**
- [ ] 第一次回复为阻断提示，包含父页面名称和连通性说明
- [ ] PM 坚持后才创建占位节点
- [ ] 占位节点格式：`[页面名\n待补充截图]`（方形节点）
- [ ] 输出末尾有占位节点提醒
- [ ] 同一对话中同一占位节点重复引用时不重复创建

---

## TC-IC-05 导入后清理：来源为 assets → 询问是否删除原始文件

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-IMP-008 |
| **覆盖 AC** | AC-5 |
| **测试目标** | 截图写入确认后，AI 询问是否删除 assets/ 中的原始文件；PM 确认则删除，保留则静默结束 |

**前置条件**
- 截图来源为 `assets/发起流程/文件上传页.png`（PM 导入前已整理）

**测试输入**（接续截图写入确认后）
```
确认写入
```
（AI 询问是否删除原始文件）
```
删除
```

**预期行为**
1. PM 确认写入后，`context/screenshots/发起流程/文件上传页.png` 写入成功
2. AI 询问：「截图已写入导航图。是否删除 `assets/发起流程/文件上传页.png` 中的原始文件？」
3. PM 回复删除
4. AI 删除 `assets/发起流程/文件上传页.png`，输出「已删除 assets/发起流程/文件上传页.png」
5. `context/screenshots/` 中的截图文件不受影响

**检查要点**
- [ ] 询问时路径含完整 assets 文件名
- [ ] 确认删除后，assets 文件被删除
- [ ] context/screenshots/ 中的文件未被删除
- [ ] PM 选择保留时静默结束，无额外输出

**负向：非 assets 来源时不询问**

**测试输入**（PM 直接粘贴截图，未整理 assets）
- 截图直接粘贴，无 assets 路径

**预期行为**：写入确认后，AI 不询问删除，直接结束

**检查要点**
- [ ] 未触发 assets 删除询问

---

## TC-IC-06 信号触发：对话含 UI 词 → 静默加载全局导航索引

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-004 |
| **覆盖 AC** | AC-6 |
| **测试目标** | 对话出现 UI 信号词时，AI 静默加载全局索引，在输出中直接引用导航内容，不暴露读取行为 |

**前置条件**
- `context/page-navigation.md` 存在，含「首页 → 发起流程入口」连边

**测试输入**（在 /prd-qa 或任意命令中）
```
发起页面的跳转逻辑是什么？
```

**预期行为**
1. AI 识别「页面」「跳转」为 UI 信号词
2. 静默读取 `context/page-navigation.md`
3. 在输出中直接引用导航关系，如「根据导航图，发起页可跳转至…」
4. 不输出「我读取了 page-navigation.md」之类的提示

**检查要点**
- [ ] 输出引用了 page-navigation.md 的导航内容
- [ ] 未向 PM 暴露读取行为（无「我读取了文件」字样）
- [ ] 同一对话中第二次出现信号词时，不重复读取同一文件

---

## TC-IC-07 信号触发：对话含模块名 → 进一步加载模块详情

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-005 |
| **覆盖 AC** | AC-7 |
| **测试目标** | 对话中出现已录入模块名时，AI 在全局索引基础上进一步加载该模块 navigation.md |

**前置条件**
- `context/page-navigation.md` 存在
- `context/screenshots/发起流程/navigation.md` 存在，含「发起页」→「签署人选择页」流程

**测试输入**
```
发起流程里签署人选择页的进入方式是什么？
```

**预期行为**
1. AI 识别「发起流程」为已录入模块名
2. 读取全局索引（CTX-NAV-004 触发）
3. 进一步静默读取 `context/screenshots/发起流程/navigation.md`
4. 输出：「根据发起流程导航图，签署人选择页从发起页点击「下一步」进入」
5. 模块文件在同一对话中只读取一次

**检查要点**
- [ ] 输出引用了模块 navigation.md 的页面级内容（非仅全局索引层面）
- [ ] 同一对话中同一模块文件不重复读取
- [ ] 模块名匹配以 context/screenshots/ 下实际目录名为准

---

## TC-IC-08 /prd-qa 导航问答：询问页面跳转 → 准确输出来源标注

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-004, CTX-NAV-005 |
| **覆盖 AC** | AC-8 |
| **测试目标** | /prd-qa 询问某页面跳转关系时，AI 准确输出跳转列表和弹窗列表，标注来源文件 |

**前置条件**
- `context/screenshots/发起流程/navigation.md` 存在，含：
  - 发起页 → 签署人选择页（点击下一步）
  - 发起页 → 签署确认弹窗（点击签署）

**测试输入**（通过 /prd-qa）
```
/prd-qa 从发起页可以跳转到哪些页面，打开哪些弹窗？
```

**预期行为**
1. AI 读取导航文件
2. 输出结构化答案：
   - 可跳转页面：签署人选择页（点击下一步）
   - 可打开弹窗：签署确认弹窗（点击签署）
3. 答案标注来源：`context/screenshots/发起流程/navigation.md`

**检查要点**
- [ ] 跳转页面和弹窗分开列出
- [ ] 包含触发方式描述
- [ ] 标注了来源文件路径

---

## TC-IC-09 飞轮：PRD 更新含新页面 → 补录建议

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-FLY-005 |
| **覆盖 AC** | AC-9 |
| **测试目标** | /update-prd 完成后，AI 扫描 §8 发现未录入导航图的新页面，在输出末尾追加补录建议 |

**前置条件**
- PRD §8 中描述了「审批详情页」（新页面，尚未在任何 navigation.md 中）

**测试输入**
```
/update-prd 审批流程 新增审批详情页，从审批列表页点击任意条目进入
```

**预期行为**
1. /update-prd 正常完成变更
2. AI 扫描更新后的 PRD §8，发现「审批详情页」未在导航图中
3. 在更新完成输出末尾追加：
   > **导航图补录建议**：检测到以下页面在 PRD §8 中描述但尚未录入导航图：
   > - 审批详情页（建议录入模块：审批流程）
   > 可通过 `/import-context` 导入对应截图补录。
4. 若导航模块文件不存在，建议注明「导入时将自动初始化」

**检查要点**
- [ ] 扫描了 §8（不含 §6 验收标准）
- [ ] 仅提议，不直接写入导航图
- [ ] 提议包含页面名称和建议模块
- [ ] 已在导航图中的页面不重复建议

---

## TC-IC-10 飞轮：原型生成后节点覆盖检查 → 缺口提示

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-FLY-006 |
| **覆盖 AC** | AC-10 |
| **测试目标** | /generate-prototype 完成后，AI 检查原型页面 vs 导航图节点，有缺口时列出 |

**前置条件**
- 原型包含 3 个页面：发起页、签署人选择页、签署结果页
- 导航图中存在：发起页、签署人选择页；缺失：签署结果页

**测试输入**
```
/generate-prototype 发起流程
```

**预期行为**
1. 原型生成完成
2. AI 对比原型页面与导航图
3. 发现「签署结果页」在原型中有但导航图中无
4. 在生成输出末尾追加：
   > **导航图覆盖检查**：以下原型页面尚未在导航图中录入：
   > - 签署结果页
   > 建议通过 `/import-context` 导入对应截图。
5. 无缺口时静默，不输出提示

**检查要点**
- [ ] 只列出缺口页面，已有节点不重复提示
- [ ] 无缺口时静默
- [ ] 导航图完全未初始化时有特定提示（「尚未建立导航图」）

---

## TC-IC-11 边界：首张截图（导航图未初始化）→ 提示自由输入父节点

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-IMP-005 |
| **测试目标** | 首次导入截图，page-navigation.md 不存在时，AI 提示描述入口而非展示空列表 |

**前置条件**
- `context/page-navigation.md` 不存在
- `context/screenshots/` 为空

**测试输入**
```
（粘贴截图）这是产品首页
```

**预期行为**
1. AI 识别为页面截图
2. 检测到导航图尚未初始化（无节点可列）
3. 输出：「当前导航图尚未初始化，请描述该页面的入口（如：首页/直接访问/应用启动后进入）」
4. 接受自由文本，以此创建首个节点（根锚点）
5. 创建 `context/page-navigation.md`，写入首个根节点

**检查要点**
- [ ] 未展示空列表
- [ ] 提示接受自由输入
- [ ] page-navigation.md 自动创建
- [ ] 首个节点成为根锚点

---

## TC-IC-12 边界：同名节点已存在 → 询问更新或别名

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-002 |
| **测试目标** | PM 导入的页面节点已存在时，AI 询问是更新还是新增别名 |

**前置条件**
- `navigation.md` 中已存在「发起页」节点

**测试输入**
```
（粘贴截图）这是发起页的新版截图，UI 已更新
```

**预期行为**
1. AI 写入步骤检测到「发起页」节点已存在
2. 询问：「节点 发起页 已存在，是更新现有节点（替换截图路径）还是新增别名节点？」
3. 等待 PM 确认后执行

**检查要点**
- [ ] 检测到同名节点
- [ ] 询问更新还是别名，未自动覆盖
- [ ] 等待 PM 确认后才写入

---

## TC-IC-13 边界：触发方式不确定 → 记录为待补充

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-IMP-006 |
| **测试目标** | PM 不确定触发方式时，连边标注「触发方式待补充」，继续写入不阻断 |

**测试输入**（触发方式询问环节）
```
不确定
```

**预期行为**
1. AI 将触发方式记录为「待补充」
2. 写入预览中连边标注：`父节点 -->|触发方式待补充| 当前页面`
3. 继续写入流程，不阻断

**检查要点**
- [ ] 未阻断流程
- [ ] 连边包含「触发方式待补充」标注
- [ ] 写入成功完成

---

## TC-IC-14 边界：全局索引超 50 行 → 写入后输出警告不阻断

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **功能编号** | CTX-NAV-001 |
| **测试目标** | 更新全局索引后行数超过 50 行，AI 输出警告但不阻断操作 |

**前置条件**
- `context/page-navigation.md` 当前已有 49 行

**预期行为**
1. 新模块入口导入，全局索引写入后变为 51 行
2. 写入完成后输出警告：「全局导航索引已超过 50 行（当前 51 行），建议检查是否有冗余内容」
3. 写入操作不回滚，正常完成

**检查要点**
- [ ] 写入正常完成（不阻断）
- [ ] 输出了超限警告
- [ ] 警告包含当前行数
